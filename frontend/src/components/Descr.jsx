import React from 'react';
import '@/styles/Descr.css';

const Descr = () => {
  return (
    <section className="hero">
      <div className="hero-content">
        <h2>A Global Platform for Formal Civil Actions</h2>
        <p>Register public declarations, create formal agreements, and access legal services, all with the security of your unique Open International ID.</p>
        <button className="btn explore">Explore More</button>
      </div>
    </section>
  );
};

export default Descr;
